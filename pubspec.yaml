name: dorm_of_decents
description: "A comprehensive bachelors flat management application built with Flutter. Track meals, expenses, settlements, and manage user activities in shared living spaces."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  toastification: ^3.0.3
  dio: ^5.9.0
  go_router: ^17.0.0
  flutter_screenutil: ^5.9.3
  shimmer: ^3.0.0
  flutter_dotenv: ^6.0.0
  equatable: ^2.0.7
  grid_background: ^0.0.1
  url_launcher: ^6.3.2
  blur: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/fonts/
    - .env

  fonts:
    - family: Geist
      fonts:
        - asset: assets/fonts/Geist-Regular.ttf
        - asset: assets/fonts/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Geist-Bold.ttf
          weight: 700
